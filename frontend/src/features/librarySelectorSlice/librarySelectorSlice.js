import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  data: [],
};

export const libarySelectorSlice = createSlice({
  name: "librarySelector",
  initialState,
  reducers: {},
});
